import React from "react"
import { Link } from "gatsby"

import "../assets/scss/card.scss"

const Card = ({ post }) => {
  return (
    <div className="w-full h-card border border-title_purple-secondary">
      hola
    </div>
  )
}

export default Card
